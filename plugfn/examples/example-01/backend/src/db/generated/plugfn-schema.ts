/**
 * Auto-generated Drizzle schema for plugfn
 * Generated by Superfunctions CLI
 * 
 * DO NOT EDIT MANUALLY
 * 
 * To regenerate: npx superfunctions generate-schema
 */

import { boolean, index, integer, json, pgTable, text, timestamp, uniqueIndex } from 'drizzle-orm/pg-core';

export const plugfn_connections = pgTable(
  'plugfn_connections',
  {
  connectedAt: timestamp('connected_at', { withTimezone: true }).notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  credentials: json('credentials').notNull(),
  delegatedToUserId: text('delegated_to_user_id'),
  expiresAt: timestamp('expires_at', { withTimezone: true }),
  grants: json('grants'),
  id: text('id').primaryKey().notNull(),
  installedByUserId: text('installed_by_user_id'),
  lastUsedAt: timestamp('last_used_at', { withTimezone: true }),
  metadata: json('metadata'),
  name: text('name'),
  organizationId: text('organization_id'),
  ownerId: text('owner_id'),
  ownerKind: text('owner_kind'),
  provider: text('provider').notNull(),
  scopes: json('scopes'),
  status: text('status').notNull(),
  tenantId: text('tenant_id'),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  userId: text('user_id').notNull(),
  },
  (table) => ({
    idx_plugfn_connections_owner: index('idx_plugfn_connections_owner').on(table.ownerKind, table.ownerId),
    idx_plugfn_connections_status: index('idx_plugfn_connections_status').on(table.status),
    idx_plugfn_connections_user_provider: index('idx_plugfn_connections_user_provider').on(table.userId, table.provider)
  })
);


export const plugfn_oauth_states = pgTable(
  'plugfn_oauth_states',
  {
  code_verifier: text('code_verifier'),
  consumed_at: text('consumed_at'),
  created_at: text('created_at').notNull(),
  expires_at: text('expires_at').notNull(),
  nonce: text('nonce'),
  provider_id: text('provider_id').notNull(),
  redirect_uri: text('redirect_uri').notNull(),
  requested_scopes: json('requested_scopes').notNull(),
  state_id: text('state_id').notNull().unique(),
  subject_key: text('subject_key').notNull(),
  subject_kind: text('subject_kind').notNull(),
  subject_payload: json('subject_payload').notNull(),
  },
  (table) => ({
    idx_plugfn_oauth_states_expires_at: index('idx_plugfn_oauth_states_expires_at').on(table.expires_at)
  })
);


export const plugfn_oauth_tokens = pgTable(
  'plugfn_oauth_tokens',
  {
  connection_id: text('connection_id').notNull().unique(),
  created_at: text('created_at').notNull(),
  encrypted_payload: text('encrypted_payload').notNull(),
  expires_at: text('expires_at'),
  key_ref: text('key_ref').notNull(),
  provider_id: text('provider_id').notNull(),
  tenant_id: text('tenant_id').notNull(),
  token_id: text('token_id').notNull().unique(),
  updated_at: text('updated_at').notNull(),
  user_id: text('user_id').notNull(),
  },
  (table) => ({
    idx_plugfn_oauth_tokens_connection: index('idx_plugfn_oauth_tokens_connection').on(table.connection_id)
  })
);


export const plugfn_workflows = pgTable(
  'plugfn_workflows',
  {
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  definition: json('definition').notNull(),
  description: text('description'),
  id: text('id').primaryKey().notNull(),
  metadata: json('metadata'),
  name: text('name').notNull(),
  status: text('status').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  userId: text('user_id').notNull(),
  },
  (table) => ({
    idx_plugfn_workflows_status: index('idx_plugfn_workflows_status').on(table.status),
    idx_plugfn_workflows_user_id: index('idx_plugfn_workflows_user_id').on(table.userId)
  })
);


export const plugfn_workflow_executions = pgTable(
  'plugfn_workflow_executions',
  {
  completedAt: timestamp('completed_at', { withTimezone: true }),
  durationMs: integer('duration_ms'),
  error: text('error'),
  id: text('id').primaryKey().notNull(),
  input: json('input'),
  output: json('output'),
  startedAt: timestamp('started_at', { withTimezone: true }).notNull(),
  status: text('status').notNull(),
  workflowId: text('workflow_id').notNull(),
  },
  (table) => ({
    idx_plugfn_workflow_executions_status: index('idx_plugfn_workflow_executions_status').on(table.status),
    idx_plugfn_workflow_executions_workflow_id: index('idx_plugfn_workflow_executions_workflow_id').on(table.workflowId)
  })
);


export const plugfn_webhooks = pgTable(
  'plugfn_webhooks',
  {
  connectionId: text('connection_id'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  events: json('events').notNull(),
  id: text('id').primaryKey().notNull(),
  metadata: json('metadata'),
  provider: text('provider').notNull(),
  secret: text('secret').notNull(),
  status: text('status').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  webhookUrl: text('webhook_url').notNull(),
  },
  (table) => ({
    idx_plugfn_webhooks_connection_id: index('idx_plugfn_webhooks_connection_id').on(table.connectionId),
    idx_plugfn_webhooks_provider: index('idx_plugfn_webhooks_provider').on(table.provider)
  })
);


export const plugfn_action_logs = pgTable(
  'plugfn_action_logs',
  {
  action: text('action'),
  cached: boolean('cached'),
  connectionId: text('connection_id'),
  durationMs: integer('duration_ms'),
  error: text('error'),
  executedAt: timestamp('executed_at', { withTimezone: true }).notNull(),
  id: text('id').primaryKey().notNull(),
  input: json('input'),
  metadata: json('metadata'),
  output: json('output'),
  provider: text('provider'),
  retries: integer('retries'),
  status: text('status'),
  userId: text('user_id'),
  },
  (table) => ({
    idx_plugfn_action_logs_connection_id: index('idx_plugfn_action_logs_connection_id').on(table.connectionId),
    idx_plugfn_action_logs_provider_action: index('idx_plugfn_action_logs_provider_action').on(table.provider, table.action)
  })
);


export const plugfn_provider_installations = pgTable(
  'plugfn_provider_installations',
  {
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  delegatedToUserId: text('delegated_to_user_id'),
  id: text('id').primaryKey().notNull(),
  installedByUserId: text('installed_by_user_id'),
  metadata: json('metadata'),
  organizationId: text('organization_id'),
  ownerId: text('owner_id').notNull(),
  ownerKind: text('owner_kind').notNull(),
  provider: text('provider').notNull(),
  scopes: json('scopes'),
  status: text('status').notNull(),
  tenantId: text('tenant_id'),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  },
  (table) => ({
    idx_plugfn_provider_installations_owner: index('idx_plugfn_provider_installations_owner').on(table.ownerKind, table.ownerId),
    idx_plugfn_provider_installations_provider: index('idx_plugfn_provider_installations_provider').on(table.provider)
  })
);


export const plugfn_connection_grants = pgTable(
  'plugfn_connection_grants',
  {
  connectionId: text('connection_id').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  expiresAt: timestamp('expires_at', { withTimezone: true }),
  grant: text('grant').notNull(),
  granteeUserId: text('grantee_user_id').notNull(),
  id: text('id').primaryKey().notNull(),
  },
  (table) => ({
    idx_plugfn_connection_grants_connection: index('idx_plugfn_connection_grants_connection').on(table.connectionId),
    idx_plugfn_connection_grants_grantee: index('idx_plugfn_connection_grants_grantee').on(table.granteeUserId)
  })
);


export const plugfn_webhook_receipts = pgTable(
  'plugfn_webhook_receipts',
  {
  connectionId: text('connection_id'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  event: text('event').notNull(),
  headersRedacted: json('headers_redacted'),
  id: text('id').primaryKey().notNull(),
  idempotencyKey: text('idempotency_key'),
  metadata: json('metadata'),
  ownerId: text('owner_id'),
  ownerKind: text('owner_kind'),
  payloadHash: text('payload_hash').notNull(),
  provider: text('provider').notNull(),
  receivedAt: timestamp('received_at', { withTimezone: true }).notNull(),
  verificationStatus: text('verification_status').notNull(),
  },
  (table) => ({
    idx_plugfn_webhook_receipts_idempotency: uniqueIndex('idx_plugfn_webhook_receipts_idempotency').on(table.provider, table.idempotencyKey),
    idx_plugfn_webhook_receipts_provider_event: index('idx_plugfn_webhook_receipts_provider_event').on(table.provider, table.event)
  })
);


export const plugfn_webhook_deliveries = pgTable(
  'plugfn_webhook_deliveries',
  {
  attempts: integer('attempts').notNull(),
  claimToken: text('claim_token'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  error: text('error'),
  handlerName: text('handler_name'),
  id: text('id').primaryKey().notNull(),
  metadata: json('metadata'),
  nextAttemptAt: timestamp('next_attempt_at', { withTimezone: true }),
  receiptId: text('receipt_id').notNull(),
  sinkId: text('sink_id'),
  status: text('status').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  },
  (table) => ({
    idx_plugfn_webhook_deliveries_receipt: index('idx_plugfn_webhook_deliveries_receipt').on(table.receiptId),
    idx_plugfn_webhook_deliveries_status: index('idx_plugfn_webhook_deliveries_status').on(table.status)
  })
);


export const plugfn_sync_jobs = pgTable(
  'plugfn_sync_jobs',
  {
  checkpoint: json('checkpoint'),
  connectionId: text('connection_id').notNull(),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  cursor: text('cursor'),
  error: text('error'),
  fetchedCount: integer('fetched_count').notNull(),
  id: text('id').primaryKey().notNull(),
  metadata: json('metadata'),
  mode: text('mode').notNull(),
  ownerId: text('owner_id'),
  ownerKind: text('owner_kind'),
  persistedCount: integer('persisted_count').notNull(),
  provider: text('provider').notNull(),
  resource: text('resource').notNull(),
  skippedCount: integer('skipped_count').notNull(),
  status: text('status').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  },
  (table) => ({
    idx_plugfn_sync_jobs_connection_resource: index('idx_plugfn_sync_jobs_connection_resource').on(table.connectionId, table.resource),
    idx_plugfn_sync_jobs_provider: index('idx_plugfn_sync_jobs_provider').on(table.provider),
    idx_plugfn_sync_jobs_status: index('idx_plugfn_sync_jobs_status').on(table.status)
  })
);


export const plugfn_sync_checkpoints = pgTable(
  'plugfn_sync_checkpoints',
  {
  checkpoint: json('checkpoint').notNull(),
  connectionId: text('connection_id').notNull(),
  id: text('id').primaryKey().notNull(),
  provider: text('provider').notNull(),
  resource: text('resource').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  },
  (table) => ({
    idx_plugfn_sync_checkpoints_connection_resource: uniqueIndex('idx_plugfn_sync_checkpoints_connection_resource').on(table.connectionId, table.resource)
  })
);


export const plugfn_provider_events = pgTable(
  'plugfn_provider_events',
  {
  connectionId: text('connection_id'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  event: text('event').notNull(),
  id: text('id').primaryKey().notNull(),
  metadata: json('metadata'),
  ownerId: text('owner_id'),
  ownerKind: text('owner_kind'),
  payload: json('payload'),
  provider: text('provider').notNull(),
  },
  (table) => ({
    idx_plugfn_provider_events_connection: index('idx_plugfn_provider_events_connection').on(table.connectionId),
    idx_plugfn_provider_events_provider_event: index('idx_plugfn_provider_events_provider_event').on(table.provider, table.event)
  })
);


export const plugfn_secret_refs = pgTable(
  'plugfn_secret_refs',
  {
  createdAt: timestamp('created_at', { withTimezone: true }).notNull(),
  id: text('id').primaryKey().notNull(),
  keyRef: text('key_ref').notNull(),
  metadata: json('metadata'),
  ownerId: text('owner_id'),
  ownerKind: text('owner_kind'),
  provider: text('provider').notNull(),
  purpose: text('purpose').notNull(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull(),
  },
  (table) => ({
    idx_plugfn_secret_refs_provider_owner: index('idx_plugfn_secret_refs_provider_owner').on(table.provider, table.ownerKind, table.ownerId),
    idx_plugfn_secret_refs_purpose: index('idx_plugfn_secret_refs_purpose').on(table.purpose)
  })
);
