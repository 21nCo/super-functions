# @devfn/compose

Starts only declared Compose services under a worktree-specific project name, injects DevFn host-port mappings while retaining conventional container ports, and never removes persistent volumes during ordinary cleanup.
