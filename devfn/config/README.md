# @devfn/config

Validated DevFn project manifests and organization port-policy contracts. TypeScript manifests are executable code and must be reviewed and explicitly trusted before `devfn up` runs them.
