---
title: "@authfn/client"
description: TypeScript client — typed methods for every plugin you've enabled.
---

# @authfn/client

`@authfn/client` is the typed browser/Node client. It uses cookie credentials by default, talks to your authfn server over HTTP, and exposes one namespace per enabled plugin (`client.password`, `client.emailOtp`, `client.session`, etc.).

> Stub page — fill in with: `createAuthClient` config, cookie vs. token mode, error model, request/response examples per plugin.
