---
title: Python SDK
description: authfn for Python servers — install, configure, mount on FastAPI/Starlette/Flask.
---

# Python SDK

The `authfn` PyPI package mirrors the TypeScript core: same plugin set, same OpenAPI surface, mounted on FastAPI, Starlette, or Flask via SQLAlchemy adapters.

> Stub page — fill in with: install, `create_authfn` config, FastAPI mounting example, Python-specific notes. Source: `authfn/python/README.md`, `QUICKSTART.md`, `INSTALLATION.md`.
