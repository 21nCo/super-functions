# Changelog

## Unreleased

- Added a Conduct v0.3 compatibility note documenting stable public subpath imports.
- Confirmed public import guidance for `credentials`, `config`, `client`, `ui`, `stdio`, and `prompt`.
