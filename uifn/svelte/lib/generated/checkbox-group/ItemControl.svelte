<script lang="ts">
  import type { CheckboxGroupController } from '@uifn/core/primitives/checkbox-group';
  import PrimitivePart from '../../internal/PrimitivePart.svelte';
  import type { SveltePrimitivePartProps } from '../../internal/compound.js';
  import { CheckboxGroupDefinition } from './definition.js';

  type Props = SveltePrimitivePartProps<CheckboxGroupController['parts']['itemControl'], 'button', true>;
  let {
    value,
    forceMount = false,
    container,
    children,
    render,
    ref = $bindable(null),
    ...rest
  }: Props = $props();
</script>

<PrimitivePart
  definition={CheckboxGroupDefinition}
  part="itemControl"
  element="button"
  many={true}
  props={rest}
  {value}
  {forceMount}
  {container}
  {children}
  {render}
  bind:ref
/>
