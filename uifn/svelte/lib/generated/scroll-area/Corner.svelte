<script lang="ts">
  import type { ScrollAreaController } from '@uifn/core/primitives/scroll-area';
  import PrimitivePart from '../../internal/PrimitivePart.svelte';
  import type { SveltePrimitivePartProps } from '../../internal/compound.js';
  import { ScrollAreaDefinition } from './definition.js';

  type Props = SveltePrimitivePartProps<ScrollAreaController['parts']['corner'], 'div', false>;
  let {
    value,
    forceMount = false,
    container,
    children,
    render,
    ref = $bindable(null),
    ...rest
  }: Props = $props();
</script>

<PrimitivePart
  definition={ScrollAreaDefinition}
  part="corner"
  element="div"
  many={false}
  props={rest}
  {value}
  {forceMount}
  {container}
  {children}
  {render}
  bind:ref
/>
