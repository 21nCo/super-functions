<script lang="ts">
  import type { ButtonContractParts } from '@uifn/core/primitives/button';
  import PrimitivePart from '../../internal/PrimitivePart.svelte';
  import type { SveltePrimitivePartProps } from '../../internal/compound.js';
  import { ButtonDefinition } from './definition.js';

  type Props = SveltePrimitivePartProps<ButtonContractParts['spinner'], 'span', false>;
  let {
    value,
    forceMount = false,
    container,
    children,
    render,
    ref = $bindable(null),
    ...rest
  }: Props = $props();
</script>

<PrimitivePart
  definition={ButtonDefinition}
  part="spinner"
  element="span"
  many={false}
  props={rest}
  {value}
  {forceMount}
  {container}
  {children}
  {render}
  bind:ref
/>
