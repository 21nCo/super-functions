<script lang="ts">
  import type { DrawerController } from '@uifn/core/primitives/drawer';
  import PrimitivePart from '../../internal/PrimitivePart.svelte';
  import type { SveltePrimitivePartProps } from '../../internal/compound.js';
  import { DrawerDefinition } from './definition.js';

  type Props = SveltePrimitivePartProps<DrawerController['parts']['portal'], 'div', false>;
  let {
    value,
    forceMount = false,
    container,
    children,
    render,
    ref = $bindable(null),
    ...rest
  }: Props = $props();
</script>

<PrimitivePart
  definition={DrawerDefinition}
  part="portal"
  element="div"
  many={false}
  props={rest}
  {value}
  {forceMount}
  {container}
  {children}
  {render}
  bind:ref
/>
