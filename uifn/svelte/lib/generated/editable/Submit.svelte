<script lang="ts">
  import type { EditableController } from '@uifn/core/primitives/editable';
  import PrimitivePart from '../../internal/PrimitivePart.svelte';
  import type { SveltePrimitivePartProps } from '../../internal/compound.js';
  import { EditableDefinition } from './definition.js';

  type Props = SveltePrimitivePartProps<EditableController['parts']['submit'], 'button', false>;
  let {
    value,
    forceMount = false,
    container,
    children,
    render,
    ref = $bindable(null),
    ...rest
  }: Props = $props();
</script>

<PrimitivePart
  definition={EditableDefinition}
  part="submit"
  element="button"
  many={false}
  props={rest}
  {value}
  {forceMount}
  {container}
  {children}
  {render}
  bind:ref
/>
