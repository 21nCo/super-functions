<script lang="ts">
  import type { TimerController } from '@uifn/core/primitives/timer';
  import PrimitivePart from '../../internal/PrimitivePart.svelte';
  import type { SveltePrimitivePartProps } from '../../internal/compound.js';
  import { TimerDefinition } from './definition.js';

  type Props = SveltePrimitivePartProps<TimerController['parts']['reset'], 'button', false>;
  let {
    value,
    forceMount = false,
    container,
    children,
    render,
    ref = $bindable(null),
    ...rest
  }: Props = $props();
</script>

<PrimitivePart
  definition={TimerDefinition}
  part="reset"
  element="button"
  many={false}
  props={rest}
  {value}
  {forceMount}
  {container}
  {children}
  {render}
  bind:ref
/>
