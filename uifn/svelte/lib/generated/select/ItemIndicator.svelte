<script lang="ts">
  import type { SelectController } from '@uifn/core/primitives/select';
  import PrimitivePart from '../../internal/PrimitivePart.svelte';
  import type { SveltePrimitivePartProps } from '../../internal/compound.js';
  import { SelectDefinition } from './definition.js';

  type Props = SveltePrimitivePartProps<SelectController['parts']['itemIndicator'], 'span', true>;
  let {
    value,
    forceMount = false,
    container,
    children,
    render,
    ref = $bindable(null),
    ...rest
  }: Props = $props();
</script>

<PrimitivePart
  definition={SelectDefinition}
  part="itemIndicator"
  element="span"
  many={true}
  props={rest}
  {value}
  {forceMount}
  {container}
  {children}
  {render}
  bind:ref
/>
