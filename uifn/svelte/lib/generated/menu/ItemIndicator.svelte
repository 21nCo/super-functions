<script lang="ts">
  import type { MenuController } from '@uifn/core/primitives/menu';
  import PrimitivePart from '../../internal/PrimitivePart.svelte';
  import type { SveltePrimitivePartProps } from '../../internal/compound.js';
  import { MenuDefinition } from './definition.js';

  type Props = SveltePrimitivePartProps<MenuController['parts']['itemIndicator'], 'span', true>;
  let {
    value,
    forceMount = false,
    container,
    children,
    render,
    ref = $bindable(null),
    ...rest
  }: Props = $props();
</script>

<PrimitivePart
  definition={MenuDefinition}
  part="itemIndicator"
  element="span"
  many={true}
  props={rest}
  {value}
  {forceMount}
  {container}
  {children}
  {render}
  bind:ref
/>
