<script lang="ts">
  import type { ComboboxController } from '@uifn/core/primitives/combobox';
  import PrimitivePart from '../../internal/PrimitivePart.svelte';
  import type { SveltePrimitivePartProps } from '../../internal/compound.js';
  import { ComboboxDefinition } from './definition.js';

  type Props = SveltePrimitivePartProps<ComboboxController['parts']['item'], 'div', true>;
  let {
    value,
    forceMount = false,
    container,
    children,
    render,
    ref = $bindable(null),
    ...rest
  }: Props = $props();
</script>

<PrimitivePart
  definition={ComboboxDefinition}
  part="item"
  element="div"
  many={true}
  props={rest}
  {value}
  {forceMount}
  {container}
  {children}
  {render}
  bind:ref
/>
