<script lang="ts">
  import type { CarouselController } from '@uifn/core/primitives/carousel';
  import PrimitivePart from '../../internal/PrimitivePart.svelte';
  import type { SveltePrimitivePartProps } from '../../internal/compound.js';
  import { CarouselDefinition } from './definition.js';

  type Props = SveltePrimitivePartProps<CarouselController['parts']['item'], 'div', true>;
  let {
    value,
    forceMount = false,
    container,
    children,
    render,
    ref = $bindable(null),
    ...rest
  }: Props = $props();
</script>

<PrimitivePart
  definition={CarouselDefinition}
  part="item"
  element="div"
  many={true}
  props={rest}
  {value}
  {forceMount}
  {container}
  {children}
  {render}
  bind:ref
/>
