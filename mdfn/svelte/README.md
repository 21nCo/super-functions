# @mdfn/svelte

Svelte 5 bindings over the shared controller. Browser editors load after the
component reaches the client and share exactly the same state and command
semantics as vanilla, React, and Solid.
