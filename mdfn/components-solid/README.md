# `@mdfn/components-solid`

Solid editor shell, complete authoring/editorial controls, and toolbar rendered
with UIFn components. The package publishes ESM/CJS browser builds plus genuine
ESM/CJS Node SSR entries and shares the canonical adapter command/file contract.
