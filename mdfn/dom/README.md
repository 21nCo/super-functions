# @mdfn/dom

The vanilla browser reference editor. It uses a proven structured-editing
kernel for selection, input, history, clipboard, composition, drag/drop, node
views, and DOM reconciliation while keeping mdfn Markdown and transactions as
the public contract.
