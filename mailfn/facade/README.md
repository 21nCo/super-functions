# `mailfn`

Convenience facade for the MailFn TypeScript client and public core types.

```ts
import { MailFnClient, type Message, type WaitForMessageResult } from 'mailfn';
```

Use the focused `@mailfn/*` packages when embedding the domain service, deploying the Cloudflare runtime, integrating tests/agents, or composing SendFn.
