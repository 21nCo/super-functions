---
title: "Resource Selectors"
description: "Parse DataFn request envelopes and collect structural resource selectors."
audience: reference
applies_to: typescript
---

Canonical protocol helpers exported by `@datafn/core` and re-exported by `@datafn/server`. See [Structural resource selectors](/docs/documentation/security/resource-selectors) for gateway and authorization guidance.

```typescript
const DATAFN_REQUEST_PROTOCOL_VERSION = "1";

type DatafnRequestAction =
  | "status"
  | "query"
  | "mutation"
  | "transact"
  | "search"
  | "seed"
  | "clone"
  | "pull"
  | "push"
  | "reconcile";

type DatafnResourceSelector = string & {
  readonly __brand: "DatafnResourceSelector";
};

type StructuralResourceSelection = {
  readonly selectors: readonly DatafnResourceSelector[];
  readonly protocolVersion: "1";
};

function parseDatafnRequest(
  action: string,
  payload: unknown,
  options?: { schema?: Pick<DatafnSchema, "resources" | "relations"> },
): DatafnEnvelope<ParsedDatafnRequest>;

function collectStructuralResourceSelectors(
  request: ParsedDatafnRequest,
): StructuralResourceSelection;

function extractStructuralResourceSelectors(
  action: string,
  payload: unknown,
  options?: { schema?: Pick<DatafnSchema, "resources" | "relations"> },
): DatafnEnvelope<StructuralResourceSelection>;
```

`parseDatafnRequest` validates protocol envelope shape and selector-field types. It does not traverse application records or filter operands. `collectStructuralResourceSelectors` is exhaustive over `ParsedDatafnRequest`.

For join-aware pull, clone and reconcile requests, pass trusted application schema metadata as the third argument:
`extractStructuralResourceSelectors("pull", payload, { schema })` (or
`parseDatafnRequest("pull", payload, { schema })`). The same option is required for `clone` and `reconcile` when `includeJoins` is enabled; omitting it returns `DFQL_UNSUPPORTED`. Standard sync reconciliation enables joins. Import `schema` from the application-owned configuration used to create the DataFn server. The parser translates known
many-to-many join cursor keys into their endpoint resources. Without schema
metadata, cursor names remain selectors; unknown `join_` names are never silently
ignored. Gateways handling relational sync must supply the same schema as the
server and authorize every returned endpoint. The server preflight supplies its
validated schema automatically; custom authorization callbacks should also pass their trusted schema to the helper.
