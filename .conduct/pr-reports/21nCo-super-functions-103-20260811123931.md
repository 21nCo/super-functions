# PR Analysis Report: 21nCo/super-functions #103 - Comprehensive Review Issues

**Generated:** 2026-08-11T12:39:31Z
**PR Title:** SF-8 Plugfn init
**PR URL:** https://github.com/21nCo/super-functions/pull/103
**Branch:** SF-8 → dev
**Files Changed:** 298 files (+44299 lines, -435 lines)

## Overview

PR #103 adds the initial PlugFn TypeScript, Python, CLI, client, provider, documentation, example, and release-gate surfaces together with shared HTTP, OAuth, middleware, database, and FileFn changes. The evidence export contains all 49 inline review comments, all 14 submitted reviews, all 7 PR conversation comments, and all 49 GraphQL review threads (35 unresolved, 14 resolved, 25 outdated). The 49 inline comments reduce to 46 unique findings after combining two multi-location contracts and one exact cross-tool duplicate.

Represented finding tools are Gemini Code Assist, Codex, cubic, GitHub Advanced Security/CodeQL, and GitHub Code Quality. SonarQube Cloud reported aggregate quality-gate failures without file-level details in the GitHub comment, Socket reported dependency scores without a concrete vulnerability, and CodeRabbit, Sourcery, Greptile, and Qodo did not complete a review because of size or account limits. Cubic also stated that its initial pass reviewed at most 100 files. Those notices are evidence limitations, not additional invented issues.

## 🔴 Critical Issues (27 Total)

### http-router.ts - Authorization
**File:** `plugfn/core/src/router/http-router.ts`

#### Issue 1: Reject request owners that do not match the authenticated user (Gemini Code Assist)
- **Severity:** High
- **Description:** A client-supplied user owner can target another user's connection, enabling cross-user operations.
- **Location:** Lines 1032-1040; discussion `3303713441`.
- **Fix:** Validate both `userId` and user-kind `owner.userId` against `authContext.userId` and return 403 on mismatch.

### action-executor.ts - Queue robustness
**File:** `plugfn/core/src/core/action-executor.ts`

#### Issue 2: Recover the per-connection queue after an earlier sync rejection (Gemini Code Assist)
- **Severity:** High
- **Description:** Awaiting a rejected prior tail can prevent every later sync for the same connection from running.
- **Location:** Lines 380-388; discussion `3303713465`.
- **Fix:** Chain and await a rejection-swallowing continuation while still releasing the current queue entry.

### token-client.ts - Optional revocation configuration
**File:** `packages/oauth-http/src/token-client.ts`

#### Issue 7: Guard both revocation URL helpers when no URL is configured (Gemini Code Assist)
- **Severity:** High
- **Description:** The URL resolver and GitHub-app predicate can call string methods on an absent optional `revocationUrl`.
- **Location:** Original lines 337-347 in two outdated threads; discussions `3303719282` and `3303719287`.
- **Fix:** Accept `string | undefined`, return an empty/no-match result when absent, and only inspect configured strings.

### workflow-engine.ts - Idempotency
**File:** `plugfn/core/src/core/workflow-engine.ts`

#### Issue 8: Make workflow idempotency concurrency-safe (Gemini Code Assist)
- **Severity:** High
- **Description:** Concurrent starts with the same idempotency key can both create and execute a workflow.
- **Location:** Original lines 144-147 in an outdated thread; discussion `3303719308`.
- **Fix:** Serialize creation by idempotency key or enforce an equivalent unique atomic storage operation.

### http-router.ts - Webhook idempotency
**File:** `plugfn/core/src/router/http-router.ts`

#### Issue 9: Deduplicate webhooks whose verification is not required (Gemini Code Assist)
- **Severity:** High
- **Description:** Existing `not-required` receipts were not treated as successfully processed, so unsigned configured flows could execute duplicates.
- **Location:** Original lines 790-800 in an outdated thread; discussion `3303719318`.
- **Fix:** Treat both `verified` and `not-required` receipts as successful duplicates.

### connection-manager.ts - OAuth refresh concurrency
**File:** `plugfn/core/src/core/connection-manager.ts`

#### Issue 10: Single-flight token refreshes per connection (Gemini Code Assist)
- **Severity:** High
- **Description:** Concurrent credential reads for one expired connection can race token rotation and trigger redundant upstream refreshes.
- **Location:** Lines 532-544; discussion `3303719310`.
- **Fix:** Share one in-flight refresh promise or mutex per connection and clean it up after settlement.

### database.ts - Defensive job claiming
**File:** `plugfn/core/src/storage/adapters/database.ts`

#### Issue 11: Null-check the delivery claim update (Gemini Code Assist)
- **Severity:** High
- **Description:** A losing worker can receive no updated row and crash while reading `updated.status`.
- **Location:** Lines 445-447; discussion `3303719343`.
- **Fix:** Only append the claim when `updated?.status === 'running'`.

### isolation-guards.ts - Tenant privacy
**File:** `plugfn/core/src/security/isolation-guards.ts`

#### Issue 12: Avoid revealing records that belong to another tenant (Gemini Code Assist)
- **Severity:** High
- **Description:** Returning not-found for absent IDs but access-denied for cross-tenant IDs creates an existence oracle.
- **Location:** Original lines 59-63 in an outdated thread; discussion `3303719331`.
- **Fix:** Return the same not-found result for absent and out-of-scope records.

### database.ts - Defensive job claiming
**File:** `plugfn/core/src/storage/adapters/database.ts`

#### Issue 13: Null-check the sync-job claim update (Gemini Code Assist)
- **Severity:** High
- **Description:** A concurrent claimant can leave the update result absent and crash the worker loop.
- **Location:** Lines 637-639; discussion `3303719352`.
- **Fix:** Only append the claim when `updated?.status === 'running'`.

### queue.ts - Memory lifecycle
**File:** `plugfn/core/src/send-governance/queue.ts`

#### Issue 16: Bound retained jobs and idempotency keys (Gemini Code Assist)
- **Severity:** High
- **Description:** Process-lifetime maps grow for every new job and idempotency key, creating a long-running memory leak.
- **Location:** Lines 24-31; discussion `3303719373`.
- **Fix:** Remove terminal entries or add bounded TTL/eviction semantics without breaking active idempotency.

### basic-usage.ts - Runnable example
**File:** `plugfn/core/examples/basic-usage.ts`

#### Issue 18: Use an encryption key that satisfies runtime validation (cubic)
- **Severity:** High
- **Description:** The example key is neither 64 hexadecimal characters nor a valid 32-byte encoded key, so initialization fails.
- **Location:** Original line 25 in a resolved, outdated thread; discussion `3303863007`.
- **Fix:** Use a valid 64-character hexadecimal example key or load one from configuration.

### express-server.ts - Webhook raw bodies
**File:** `plugfn/core/examples/express-server.ts`

#### Issue 19: Keep global JSON parsing from corrupting webhook signature input (cubic)
- **Severity:** High
- **Description:** Parsing JSON before PlugFn can replace the provider's raw bytes and invalidate signature verification.
- **Location:** Original line 13 in a resolved, outdated thread; discussion `3303863014`.
- **Fix:** Preserve raw PlugFn webhook bodies while arranging parsing for other JSON routes.

### basic-auth.ts - Credential parsing
**File:** `plugfn/core/src/auth/basic-auth.ts`

#### Issue 21: Preserve colons inside Basic-auth passwords (cubic)
- **Severity:** High
- **Description:** Splitting on every colon truncates valid passwords that contain `:`.
- **Location:** Original line 39 in a resolved, outdated thread; discussion `3303863048`.
- **Fix:** Split at the first colon and retain the full suffix as the password.

### adapter.ts - Dynamic Hono mounting
**File:** `packages/http-hono/src/adapter.ts`

#### Issue 23: Strip dynamic mount prefixes using resolved path values (Codex)
- **Severity:** High
- **Description:** Comparing a route pattern such as `/:tenant/*` with a concrete URL leaves the prefix intact and makes mounted routes return 404.
- **Location:** Lines 114-115; discussion `3303770920`.
- **Fix:** Resolve parameterized mount segments against the request path before rewriting the forwarded URL.

### rate-limit.ts - Retry correctness
**File:** `packages/middleware/src/rate-limit.ts`

#### Issue 24: Return the latest blocked reset for multi-key denial (Codex)
- **Severity:** High
- **Description:** Returning the earliest reset can tell a caller to retry while another required key is still blocked.
- **Location:** Original lines 374-375 in an outdated thread; discussion `3304497093`.
- **Fix:** Use the maximum blocked reset time when the combined check fails.

### init.ts - Generated package imports
**File:** `plugfn/cli/src/commands/init.ts`

#### Issue 25: Scaffold the published PlugFn package names (Codex)
- **Severity:** High
- **Description:** Generated projects import nonexistent `@superfunctions/plugfn` packages instead of `plugfn` and `@plugfn/providers`.
- **Location:** Original line 30 in an outdated thread; discussion `3304497102`.
- **Fix:** Update every generated import specifier to the package names published by this PR.

### connection-manager.ts - Regular-expression denial of service
**File:** `plugfn/core/src/core/connection-manager.ts`

#### Issue 27: Remove polynomial regular expressions from library-input parsing (GitHub Advanced Security/CodeQL)
- **Severity:** High
- **Description:** CodeQL found a regex whose runtime can grow excessively on uncontrolled strings with repeated `/` characters.
- **Location:** Original line 1109 in a resolved, outdated thread; discussion `3304551999`.
- **Fix:** Replace the vulnerable regex parsing with bounded string operations or a linear-time expression.

### queue.ts - Failed enqueue rollback
**File:** `plugfn/core/src/send-governance/queue.ts`

#### Issue 28: Roll back local queue state when the adapter enqueue fails (cubic)
- **Severity:** High
- **Description:** Persisting maps before the awaited adapter call can leave ghost jobs and stale idempotency records.
- **Location:** Original line 67 in a resolved, outdated thread; discussion `3304622495`.
- **Fix:** Delete all locally inserted state on adapter failure or publish state only after a successful enqueue.

### adapter.ts - Encoded dynamic paths
**File:** `packages/http-hono/src/adapter.ts`

#### Issue 30: Preserve encoded path segments while resolving dynamic mounts (cubic)
- **Severity:** High
- **Description:** Preferring decoded route params can make the computed prefix differ from the encoded URL and prevent stripping.
- **Location:** Original line 95 in a resolved, outdated thread; discussion `3304622537`.
- **Fix:** Prefer the actual URL path segment and use decoded params only as a fallback.

### action-executor.ts - Cache safety
**File:** `plugfn/core/src/core/action-executor.ts`

#### Issue 32: Do not cache mutating actions by default (Codex)
- **Severity:** High
- **Description:** Repeating a create/update/delete action can return a cached result without sending the second provider request.
- **Location:** Original lines 113-115 in an outdated thread; discussion `3312820397`.
- **Fix:** Restrict default caching to read-only/cacheable actions or require explicit opt-in for side-effecting actions.

#### Issue 33: Include the resolved connection identity in cache keys (Codex)
- **Severity:** High
- **Description:** Two accounts for one user and provider can share cached results, returning data from the wrong connection.
- **Location:** Original lines 105-110 in an outdated thread; discussion `3312820399`.
- **Fix:** Resolve the connection before lookup and include its stable ID in the cache key.

### fastapi.py - Canonical OAuth callback
**File:** `plugfn/python/plugfn/adapters/fastapi.py`

#### Issue 38: Allow callbacks that identify the provider through OAuth state (Codex)
- **Severity:** High
- **Description:** Making the `provider` query parameter required rejects normal `code` and `state` callbacks with 422.
- **Location:** Line 43; discussion `3315668800`.
- **Fix:** Make `provider` optional and let the shared callback handler resolve it from stored state.

### flask.py - Canonical OAuth callback
**File:** `plugfn/python/plugfn/adapters/flask.py`

#### Issue 39: Allow callbacks that omit a nonstandard provider query parameter (Codex)
- **Severity:** High
- **Description:** The canonical Flask route forwards no provider and fails validation even though OAuth state identifies the integration.
- **Location:** Line 52; discussion `3315668803`.
- **Fix:** Make the route's provider optional and resolve it from stored state in the shared handler.

### plug-fn.ts - Principal normalization
**File:** `plugfn/core/src/core/plug-fn.ts`

#### Issue 41: Guard malformed auth-session subjects (cubic)
- **Severity:** High
- **Description:** Reading `subject.actorId` before confirming `subject` is a non-null object can throw for malformed sessions.
- **Location:** Lines 1174-1175; discussion `3315695462`.
- **Fix:** Check that `subject` is a non-null object before reading and validating `actorId`.

### queue.ts - Concurrent idempotency publication
**File:** `plugfn/core/src/send-governance/queue.ts`

#### Issue 42: Do not expose duplicate state before enqueue durability (cubic)
- **Severity:** High
- **Description:** A concurrent request can observe `duplicate: true` while the first adapter write is still pending and later rolls back.
- **Location:** Line 69; discussion `3315695478`.
- **Fix:** Serialize same-key enqueues or publish the idempotency mapping only after the adapter write succeeds.

### express-server.ts - JSON request handling
**File:** `plugfn/core/examples/express-server.ts`

#### Issue 43: Parse JSON before PlugFn non-webhook POST routes without losing raw webhook bytes (cubic)
- **Severity:** High
- **Description:** Mounting PlugFn before any JSON parser leaves ordinary PlugFn POST routes with empty bodies.
- **Location:** Line 68; discussion `3315695480`.
- **Fix:** Use route-aware parsing/raw capture so PlugFn API POST bodies are decoded while webhook verification still receives exact bytes.

### action-executor.ts - Default caching
**File:** `plugfn/core/src/core/action-executor.ts`

#### Issue 44: Restore the documented default cache behavior (cubic)
- **Severity:** High
- **Description:** Requiring `options.cache` to be defined disables caching for normal calls and `batch()` operations.
- **Location:** Lines 103-104; discussion `3315695481`.
- **Fix:** Default to cache enabled when globally configured and not explicitly disabled, while retaining the side-effect safety from Issue 32.

## 🟡 Medium Severity Issues (17 Total)

### database.ts - Defensive claiming
**File:** `plugfn/core/src/storage/adapters/database.ts`

#### Issue 3: Null-check the first claim update result (Gemini Code Assist)
- **Severity:** Medium
- **Description:** A no-match update can return no row and make `updated.status` throw.
- **Location:** Lines 432-434; discussion `3303713476`.
- **Fix:** Only append when `updated?.status === 'running'`.

### adapter.ts - Missing route context
**File:** `packages/http-hono/src/adapter.ts`

#### Issue 4: Guard absent or invalid route paths (Gemini Code Assist)
- **Severity:** Medium
- **Description:** Calling `endsWith` on an absent Hono route path can crash the adapter.
- **Location:** Original lines 54-60 in an outdated thread; discussion `3303713470`.
- **Fix:** Accept an optional path and return an empty mount prefix when it is not a non-empty string.

### database.ts - Defensive claiming
**File:** `plugfn/core/src/storage/adapters/database.ts`

#### Issue 5: Null-check the second claim update result (Gemini Code Assist)
- **Severity:** Medium
- **Description:** Another worker can win the claim and leave no updated row to inspect.
- **Location:** Lines 479-481; discussion `3303713485`.
- **Fix:** Only append when `updated?.status === 'running'`.

### caching.ts and action-executor.ts - Cache miss contract
**File:** `plugfn/core/src/middleware/caching.ts`, `plugfn/core/src/core/action-executor.ts`

#### Issue 6: Distinguish a cache miss from a cached null value (Gemini Code Assist)
- **Severity:** Medium
- **Description:** Treating `null` as the miss sentinel prevents legitimate null results from being cached; the caller must use the same sentinel contract.
- **Location:** Original lines 28-42 and 108-109 in two outdated threads; discussions `3303713489` and `3303713504`.
- **Fix:** Use an explicit miss representation consistently in the cache and executor without breaking legitimate `null` values.

### action-executor.ts - Atomic rate limiting
**File:** `plugfn/core/src/core/action-executor.ts`

#### Issue 14: Avoid leaking the first token when a second limiter blocks (Gemini Code Assist)
- **Severity:** Medium
- **Description:** Sequential tenant/provider acquisition can consume one bucket even when the combined operation is denied.
- **Location:** Lines 140-157; discussion `3303719358`.
- **Fix:** Acquire all required keys atomically or roll back earlier consumption on failure.

### redaction.ts - Circular structures
**File:** `plugfn/core/src/security/redaction.ts`

#### Issue 15: Make redaction safe for circular objects (Gemini Code Assist)
- **Severity:** Medium
- **Description:** Recursive traversal without cycle tracking can overflow the stack.
- **Location:** Line 94; discussion `3303719367`.
- **Fix:** Track the active traversal chain and emit a safe circular marker.

### plug-fn.ts - Cancellation
**File:** `plugfn/core/src/core/plug-fn.ts`

#### Issue 17: Stop page fetching after a sync job is cancelled (Gemini Code Assist)
- **Severity:** Medium
- **Description:** Provider sync pagination continues consuming resources after cancellation.
- **Location:** Lines 913-914; discussion `3303719380`.
- **Fix:** Check current job status or an abort signal before every page fetch.

### examples/README.md - Example command
**File:** `plugfn/core/examples/README.md`

#### Issue 20: Run the TypeScript example with a valid command (cubic)
- **Severity:** Medium
- **Description:** `node examples/basic-usage.js` points to a file that is not produced at that location.
- **Location:** Original line 19 in a resolved, outdated thread; discussion `3303863025`.
- **Fix:** Use a TypeScript runner or reference the actual compiled output path.

### redaction.ts - Shared references
**File:** `plugfn/core/src/security/redaction.ts`

#### Issue 29: Do not label repeated non-cyclic references as circular (cubic)
- **Severity:** Medium
- **Description:** A process-wide visited set can replace later shared references with `[Circular]` even when no cycle exists.
- **Location:** Line 106 in a resolved thread; discussion `3304622534`.
- **Fix:** Track only the active recursion path and remove objects when unwinding.

### caching.ts - Unsupported invalidation
**File:** `plugfn/core/src/middleware/caching.ts`

#### Issue 31: Fail honestly when a KV-backed cache cannot be cleared (cubic)
- **Severity:** Medium
- **Description:** A silent no-op lets callers believe stale KV entries were invalidated.
- **Location:** Original lines 120-121 in a resolved, outdated thread; discussion `3304622548`.
- **Fix:** Throw an explicit unsupported-operation error or provide real prefix invalidation.

### caching.ts - TTL semantics
**File:** `plugfn/core/src/middleware/caching.ts`

#### Issue 34: Preserve an explicit zero TTL (cubic)
- **Severity:** Medium
- **Description:** Boolean fallback replaces `0` with the default TTL.
- **Location:** Original line 89 in a resolved, outdated thread; discussion `3312842352`.
- **Fix:** Use nullish coalescing for the default.

#### Issue 35: Preserve legitimate undefined cache results (cubic)
- **Severity:** Medium
- **Description:** Using `undefined` as the sole miss sentinel causes valid undefined results to recompute forever.
- **Location:** Original line 155 in a resolved, outdated thread; discussion `3312842368`.
- **Fix:** Represent hit/miss explicitly rather than overloading a cacheable value.

### metrics.ts - Telemetry isolation
**File:** `plugfn/core/src/middleware/metrics.ts`

#### Issue 36: Keep telemetry backend failures from breaking action recording (cubic)
- **Severity:** Medium
- **Description:** A synchronous `emitter.track()` exception can escape the metrics path.
- **Location:** Original line 34 in a resolved, outdated thread; discussion `3312842371`.
- **Fix:** Isolate emitter errors while still recording the local metric.

### rate-limit.ts - Partial writes
**File:** `packages/middleware/src/rate-limit.ts`

#### Issue 37: Prevent partial quota consumption during multi-key commit (cubic)
- **Severity:** Medium
- **Description:** Parallel storage writes can consume some keys if another write fails.
- **Location:** Original line 380 in a resolved, outdated thread; discussion `3312842374`.
- **Fix:** Use atomic storage when available or restore every previously committed state on failure.

### rate-limiter.ts - Per-key snapshots
**File:** `plugfn/core/src/middleware/rate-limiter.ts`

#### Issue 40: Preserve each key's own reset time after acquireMany (Codex; independently repeated by cubic)
- **Severity:** Medium
- **Description:** Assigning one aggregate reset to every bucket can make `wouldExceed` permit a still-depleted provider bucket.
- **Location:** Lines 74-76; discussions `3315668806` and duplicate `3315695494`.
- **Fix:** Return and store reset times per key or recompute each snapshot from its actual bucket state.

### plug-fn.ts - Fallback sync cancellation
**File:** `plugfn/core/src/core/plug-fn.ts`

#### Issue 45: Honor cancellation before fallback action-based sync (cubic)
- **Severity:** Medium
- **Description:** Cancellation checks cover provider sync definitions but not the fallback action path.
- **Location:** Line 624; discussion `3315695488`.
- **Fix:** Invoke the same continuation check before executing the fallback action.

### rate-limit.ts - Rollback expiry
**File:** `packages/middleware/src/rate-limit.ts`

#### Issue 46: Restore original remaining TTLs after partial commit failure (cubic)
- **Severity:** Medium
- **Description:** Rewriting previous states with a full window can extend rate-limit blocks.
- **Location:** Line 419; discussion `3315695491`.
- **Fix:** Capture each prior state's remaining expiry and use that TTL during rollback.

## 🟢 Low Severity Issues (2 Total)

### oauth-flow.ts - Deprecation diagnostics
**File:** `plugfn/core/src/auth/oauth-flow.ts`

#### Issue 22: Emit the legacy-path warning with a real warning code (cubic)
- **Severity:** Low
- **Description:** Passing the code in the warning-type slot prevents consumers from observing the intended code.
- **Location:** Original line 21 in a resolved, outdated thread; discussion `3303863055`.
- **Fix:** Use the options form with `type: 'DeprecationWarning'` and `code`.

### connection-manager.ts - Type guard clarity
**File:** `plugfn/core/src/core/connection-manager.ts`

#### Issue 26: Avoid the CodeQL null-comparison warning in the resolver guard (GitHub Code Quality)
- **Severity:** Low
- **Description:** The analyzer reports an inconvertible comparison in the object/null guard.
- **Location:** Line 1102; discussion `3304475095`.
- **Fix:** Use a truthiness guard before `typeof value === 'object'` and the property check.

## Issues by Tool
| Tool | Critical | Medium | Low | Total |
|---|---:|---:|---:|---:|
| Gemini Code Assist | 10 | 7 | 0 | 17 |
| Codex | 7 | 1 | 0 | 8 |
| cubic | 9 | 9 | 1 | 19 |
| GitHub Advanced Security/CodeQL | 1 | 0 | 0 | 1 |
| GitHub Code Quality | 0 | 0 | 1 | 1 |
| **Total** | **27** | **17** | **2** | **46** |

## Priority Action Items

### Immediate Fixes Required (Critical)
1. Close cross-user and cross-tenant authorization leaks (Issues 1, 12, and 33).
2. Make concurrency and idempotency paths durable (Issues 2, 8-10, 24, 28, and 42).
3. Repair broken OAuth callbacks, generated imports, mounting, parsing, and cache behavior (Issues 7, 18-19, 23, 25, 30, 32, 38-39, and 41-44).
4. Eliminate worker crashes, memory growth, and the CodeQL regex risk (Issues 11, 13, 16, and 27).

### Important Improvements (Medium Priority)
1. Complete defensive claim handling and atomic multi-key rate limiting (Issues 3, 5, 14, 37, 40, and 46).
2. Make caching represent hits, misses, null, undefined, TTL zero, and invalidation honestly (Issues 6, 31, 34, and 35).
3. Complete cancellation, redaction, route-path, example, and telemetry robustness (Issues 4, 15, 17, 20, 29, 36, and 45).

### Code Quality (Low Priority)
1. Correct the deprecation warning metadata (Issue 22).
2. Simplify the resolver object guard to satisfy the analyzer (Issue 26).

## Testing Recommendations

Add or run focused tests for cross-user owner rejection; concurrent refresh/workflow/enqueue calls; losing database claims; null/undefined/zero-TTL caching; multiple connections; mutating action repeats; circular and shared-reference redaction; dynamic and encoded Hono mounts; raw webhook signatures plus JSON API bodies; canonical FastAPI/Flask callbacks; cancelled provider and fallback sync; per-key limiter resets and rollback TTLs; queue eviction; and Basic-auth passwords containing colons. Run the PlugFn TypeScript and Python suites, the affected shared-package suites, typechecks, and release/provider gates after targeted checks pass.

## Summary

The fetched evidence supports 46 unique issues: 27 high, 17 medium, and 2 low. The primary risks are authorization isolation, concurrency/idempotency, routing and OAuth callback breakage, cache correctness, webhook/body handling, and rate-limit consistency. Several older threads are already marked resolved or outdated and must be verified against the exact current worktree rather than reimplemented blindly. Coverage remains incomplete because multiple bots skipped this 298-file PR and the Sonar/Socket GitHub comments expose only aggregate results.
